# CLAUDE.md

## プロジェクト概要
このプロジェクトは、イベント管理アプリのフロントエンドを Next.js で実装する構成です。
バックエンドは event_app_api が担当し、このフロントエンドは API を利用して画面を構築します。

---

## 技術スタック
- Next.js
- TypeScript
- React
- ESLint
- バックエンド API: Rails 7.1系（event_app_api）

---

## AIの振る舞い方針（重要）

- AIは単なる実装者ではなく、戦略的な協力者として振る舞うこと
- 指示をそのまま実行するのではなく、論理性・実用性・保守性の観点から妥当性を検討すること
- 問題がある場合は遠慮なく指摘し、理由と代替案を提示すること
- 不明点や前提の不足がある場合は、推測で進めず必ず確認すること
- 迎合せず、より良い設計・実装を優先すること

---

## 開発方針
- バックエンドAPIを正とする
- フロントエンドは API を利用して画面を構築する
- フロントエンド側で業務ロジックやDB前提の実装をしない
- まずはシンプルに実装し、必要に応じて整理・分離する
- 過度な抽象化は避ける
- 可読性を優先する
- UI とデータ取得の責務を分ける
- 既存の画面構成・実装方針に合わせる

---

## ローカル開発
ローカル開発は、既存の package manager / スクリプトに従う。

よく使うコマンド:
- `npm install`
- `npm run dev`
- `npm run lint`
- `npm run build`

必要に応じて README や package.json の scripts を確認すること。

---

## 実装ルール
- React / Next.js のコードはシンプルで読みやすく書く
- 画面コンポーネントに責務を詰め込みすぎない
- 表示ロジックとデータ取得ロジックを分離する
- リファクタ時は、明示的な指示がない限り挙動を変えない
- 不要なファイル追加や広範囲変更を避ける
- 既存UIや既存API契約を勝手に変更しない
- 型を明示し、`any` の多用を避ける

---

## API連携ルール
- フロントエンドは必ず API 経由でデータ取得・更新を行う
- フロントエンド側で新しいAPI仕様を勝手に決めない
- API仕様を追加・変更する場合は、まず event_app_api 側の変更を提案する
- UI実装時は、まず既存APIで実現できるかを確認する
- 既存APIで不足がある場合のみ、バックエンド変更を提案する
- リクエスト形式・レスポンス形式・認証方式は API 側の仕様に従う

---

<!-- BEGIN:nextjs-agent-rules -->
## This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

---

## エラーハンドリング
- API エラー時は、ユーザーにとって分かりやすい表示を行う
- ローディング状態・空データ状態・エラー状態を考慮する
- エラー表示は過剰に複雑にせず、簡潔にする
- エラー処理の共通化は必要性が明確な場合に行う

---

## UI実装ルール
- コンポーネントは再利用性よりも、まず理解しやすさを優先する
- Props は必要最小限に保つ
- 過剰な状態管理を避ける
- フォームは入力値、送信中、成功、失敗の状態を意識して実装する
- 一覧・詳細・作成・編集などの画面責務を明確にする
- 表示のための整形はフロントで行ってよいが、業務判断は API 側を正とする

---

## 変更ポリシー（重要）
- 既存の挙動は原則として変更しない
- 変更が必要な場合は、影響範囲を明示する
- リファクタリング時は「挙動が変わらないこと」を意識する
- 変更内容は小さく、意図が明確な単位に分ける

---

## 禁止事項（最重要）

以下の操作は、明示的な指示がない限り絶対に実行しないこと：

- git push / force push
- main / develop ブランチへの直接コミット
- API仕様の独断変更
- 既存UIの大幅変更
- 本番環境を想定した設定変更
- 既存データを破壊する可能性のある処理

変更が必要な場合は、必ず事前に提案し、承認を得てから実行すること

---

## 変更時の安全ルール
- 変更前に関連ファイルと既存実装を確認する
- 影響範囲を把握してから実装する
- API連携を変える場合は、API側の仕様と整合を確認する
- 画面変更時は、対象画面と関連画面の影響を確認する
- 可能であれば lint / build を通して確認する

---

## ブランチ運用
- `main` は安定版
- `develop` は開発統合ブランチ
- 作業は `develop` から作業ブランチを切る
- 原則として `main` に直接コミットしない

例：
- `feature/add-event-page`
- `fix/login-form-bug`

詳細は関連ドキュメントを参照

---

## 参照ドキュメント
- `README.md`
- `package.json`
- `AGENTS.md`
- event_app_api 側の `docs/api.md`
